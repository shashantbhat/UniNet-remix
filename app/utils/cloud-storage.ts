export async function uploadToCloudStorage(file: File) {
    // Example: Upload to Firebase or AWS S3
    // Replace with your own implementation
    const fileUrl = `https://your-cloud-storage.com/${file.name}`;
    return fileUrl;
  }